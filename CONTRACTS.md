# CONTRACTS.md — قراردادهای قفل‌شده

> این دو قرارداد مرز بین کارهای موازی تیم‌اند. تا وقتی ثابت بمانند، چهار نفر
> می‌توانند بدون منتظر ماندن برای هم کار کنند.
>
> **تغییر بدون هماهنگی تیم ممنوع.** تغییر قرارداد داده یعنی اجرای دوباره ingest
> روی ۱۱۴۲ فایل؛ تغییر قرارداد API یعنی شکستن UI.

---

## ۱. قرارداد داده — یک «تکه»

منبع رسمی: [`ingest/schema.py`](ingest/schema.py)

```json
{
  "id": "paas-django-deploy-app#liara-json#cli",
  "text": "متنی که به LLM داده می‌شود",
  "url": "https://docs.liara.ir/paas/django/how-tos/deploy-app/#liara-json",
  "page_title": "استقرار برنامه Django در لیارا",
  "section_title": "فایل liara.json",
  "breadcrumb": "پلتفرم (PaaS) › django › راهنماها",
  "service": "paas",
  "variant": "Liara CLI",
  "has_code": true,
  "source_path": "src/pages/paas/django/how-tos/deploy-app.mdx"
}
```

| فیلد | توضیح |
|---|---|
| `id` | یکتا. قالب: `{اسلاگ-صفحه}#{anchor-بخش}#{روش}` |
| `text` | متن خام بخش. همین به context مدل می‌رود. |
| `url` | لینک کامل **با anchor بخش** — نه صرفاً لینک صفحه |
| `page_title` | عنوان `# H1` صفحه |
| `section_title` | عنوان `<Section title="...">` |
| `breadcrumb` | مسیر خوانا برای نمایش و برای غنی‌سازی متن امبدینگ |
| `service` | یکی از ۱۱ بخش سطح‌اول. برای فیلتر بازیابی. |
| `variant` | برچسب `<Tabs>` — ۷۷ مقدار مختلف: روش استقرار، فریم‌ورک، زبان، سیستم‌عامل، پروتکل، … |
| `has_code` | آیا تکه بلوک کد دارد |
| `source_path` | مسیر فایل اصلی، فقط برای دیباگ |

### دو تصمیم که ممکن است عجیب به نظر برسند

**`embed_text` در `chunks.json` نیست.** متنی که وکتور می‌شود سرآیند زمینه دارد
(`مسیر › صفحه › بخش`)، ولی متنی که به LLM می‌رود نباید داشته باشد — وگرنه context
با تکرار پر می‌شود. `embed_text` فقط در build time در حافظه ساخته می‌شود و
ذخیره نمی‌شود؛ این تنهایی حجم `chunks.json` را تقریباً نصف می‌کند.

**`method` جدا نگه داشته می‌شود.** صفحات استقرار در مستندات لیارا `<Tabs>` دارند
با محتوای متفاوت برای کنسول / CLI / گیت‌هاب. اگر ادغام شوند، هر پنج جای `top-k`
با تکرار یک مطلب پر می‌شود. جدا نگه‌داشتن‌شان هم بازیابی را تمیز می‌کند و هم
پایه‌ی سؤال تکمیلی «با کدام روش؟» را می‌دهد.

---

## ۲. قرارداد API

منبع رسمی: [`app/contracts.py`](app/contracts.py)

### درخواست

```http
POST /api/chat
Content-Type: application/json

{ "message": "نسخه پایتون رو کجا تعیین کنم؟", "session_id": "uuid", "client_id": "browser-uuid" }
```

### پاسخ — `text/event-stream`

```
event: tool
data: {"name":"search_docs","status":"running","detail":"در حال جستجوی مستندات"}

event: tool
data: {"name":"search_docs","status":"done","detail":"۳ نتیجه مرتبط"}

event: token
data: {"t":"برای "}

event: sources
data: {"items":[{"title":"...","section":"...","url":"...","service":"paas"}]}

event: done
data: {"tokens_used":620,"cached":false,"latency_ms":2450}
```

### هفت رویداد

| رویداد | چه وقت | برای چه |
|---|---|---|
| `token` | حین تولید | نمایش توکن‌به‌توکن |
| `tool` | شروع و پایان هر ابزار | **دیده شدن** قابلیت Agentic |
| `flow` | هر نوبتی که فرآیند چندمرحله‌ای فعال است | نمایش stepper و قدم جاری |
| `sources` | یک‌بار، قبل از `done` | کارت منابع زیر پاسخ |
| `suggestions` | یک‌بار، بعد از `sources` | چیپ‌های «قدم بعدی» |
| `done` | آخر | مصرف توکن، latency، وضعیت کش |
| `error` | در صورت خطا | پیام خطای قابل نمایش |

> `flow` و `suggestions` **افزوده**اند: کلاینتی که آن‌ها را نمی‌شناسد
> نادیده‌شان می‌گیرد و دقیقاً مثل قبل کار می‌کند. هیچ رویداد قبلی عوض نشده.

### دو رویداد جدید

```
event: flow
data: {"id":"deploy_app","title":"استقرار برنامه از صفر تا اجرا",
       "step":2,"total":6,"status":"advanced",
       "steps":[{"index":1,"title":"ساخت برنامه در کنسول لیارا","status":"done"},
                {"index":2,"title":"آماده‌سازی پروژه Django","status":"current"},
                {"index":3,"title":"نوشتن فایل liara.json","status":"pending"}]}

event: suggestions
data: {"items":[
  {"label":"قدم بعد: نوشتن فایل liara.json","prompt":"قدم بعد","kind":"step"},
  {"label":"خروج از فرآیند","prompt":"خروج از فرآیند","kind":"step"}]}
```

| فیلد | مقادیر مجاز |
|---|---|
| `flow.status` | `started` \| `advanced` \| `completed` \| `exited` |
| `flow.steps[].status` | `done` \| `current` \| `pending` |
| `flow.step` | قدم جاری، **۱-پایه**. برای `completed` برابر `0` است. |
| `suggestion.kind` | `ask` (سؤال معمولی) \| `flow` (شروع فرآیند) \| `step` (ناوبری فرآیند) |

`label` روی چیپ نوشته می‌شود، `prompt` چیزی است که با کلیک ارسال می‌شود. این دو
عمداً جدا هستند: برچسب باید کوتاه باشد ولی پیام باید بدون زمینه هم معنا بدهد.

### فرآیند چندمرحله‌ای — قرارداد رفتاری

- فرآیند فقط با درخواست **صریح** کاربر شروع می‌شود: نشانه‌ی مسیر
  («قدم به قدم»، «از صفر»، «راهنمای کامل») **به‌علاوه‌ی** موضوع. یک سؤال ساده
  هرگز کاربر را وارد فرآیند نمی‌کند.
- ترتیب و عنوان قدم‌ها در `app/flows.py` قطعی است؛ مدل حق ساختن، حذف یا
  جابه‌جا کردن قدم را ندارد. متن هر قدم از مستندات همان قدم بازیابی می‌شود.
- «قدم بعد» / «قدم قبل» / «خروج از فرآیند» به‌صورت قاعده‌محور تشخیص داده
  می‌شوند — بدون تماس LLM.
- وضعیت فرآیند در ستون `flow_json` جدول `sessions` می‌ماند، پس کاربر می‌تواند
  فردا از همان قدم ادامه بدهد.
- سؤال آزاد وسط فرآیند، فرآیند را لغو نمی‌کند: پاسخ عادی داده می‌شود و
  stepper سر جایش می‌ماند.

### حافظه‌ی گفتگو — چه چیزی یادش می‌ماند

آنچه کاربر یک بار گفته، نباید دوباره پرسیده یا فراموش شود. سه لایه:

| لایه | کجا ذخیره می‌شود | عمر |
|---|---|---|
| پروفایل فرم پروژه | `sessions.profile_json` | تا آخر گفتگو |
| پلتفرمی که کاربر در پیام گفته | `sessions.platform` | تا آخر گفتگو |
| سیگنال‌های فرآیند (پلتفرم، موتور دیتابیس، روش) | `flow_json.hints` | تا پایان فرآیند |

ترتیب اولویت هنگام ساخت پاسخ یا قدم:

```
پیام فعلی → پروفایل فرم → hint فرآیند → sessions.platform → بقیه‌ی پیام‌های کاربر
```

پیام فعلی اول است چون تازه‌ترین حرف صریح کاربر است؛ بقیه حافظه است.

**چرا لایه‌ی سوم لازم است:** پیام «قدم بعد» هیچ کلمه‌ی مفیدی ندارد. بدون
حافظه، کاربری که در پیام اول گفته «داکر»، در قدم دوم راهنمای عمومی PHP و Go
می‌گیرد. `hints` داخل خود فرآیند ذخیره می‌شود تا حتی بعد از خارج شدن پیام اول
از پنجره‌ی context هم باقی بماند.

### قانون سؤال تکمیلی

سؤال تکمیلی فقط وقتی منتشر می‌شود که **گزینه** داشته باشد (دست‌کم دو تا). یک
سؤال باز مثل «چه نوع سرویسی می‌خواهید؟» کار را به کاربر پس می‌دهد بدون اینکه
بگوید جواب‌های ممکن چیست، و کاربر همان جمله‌ی قبلی را دوباره می‌نویسد.

این قاعده در `app/agent.py::usable_clarification` **تضمین** شده، نه فقط در
پرامپت: اگر مدل سؤال بی‌گزینه برگرداند، سؤال دور ریخته می‌شود و جستجوی عادی
انجام می‌شود. گزینه‌ها در همان `suggestions` به‌صورت چیپ می‌آیند تا کاربر با
یک کلیک جواب بدهد.

همچنین وقتی کاربر **قبلاً جواب داده** — «یه سرویس مبتنی بر داکر بیارم بالا»
پلتفرم را گفته — هیچ سؤالی پرسیده نمی‌شود: `platform_deploy_route` قاعده‌محور
مستقیم به مستندات همان پلتفرم می‌رود.

### پیام‌های ذخیره‌شده

`GET /api/sessions/{id}` علاوه بر `sources`، حالا `suggestions` هر پیام را هم
برمی‌گرداند. فرانت فقط چیپ‌های **آخرین** پیام را رندر می‌کند.

### چرا `tool` جدا از `token` است

اگر ربات پشت صحنه ابزار صدا بزند و کاربر فقط یک اسپینر ببیند، آن ۵۰ امتیاز
Agentic عملاً دیده نمی‌شود. داور باید روی صفحه ببیند «در حال جستجوی مستندات…»
و «تحلیل خطا…». قابلیتی که نمایش داده نشود، امتیاز نمی‌گیرد.

### خطاها

| کد HTTP | `code` | معنی |
|---|---|---|
| ۴۱۳ | `message_too_long` | پیام از سقف بلندتر است |
| ۴۲۹ | `rate_limited` | سقف نرخ رد شده (فاز ۵) |
| ۵۰۰ | `internal_error` | خطای داخلی |

### تاریخچه گفتگو

| متد | مسیر | کاربرد |
|---|---|---|
| `GET` | `/api/sessions?client_id=...` | فهرست گفتگوهای همان مرورگر |
| `POST` | `/api/sessions` | ساخت گفتگوی جدید |
| `GET` | `/api/sessions/{id}?client_id=...` | پیام‌ها و منابع یک گفتگو |
| `PATCH` | `/api/sessions/{id}` | تغییر نام گفتگو |
| `DELETE` | `/api/sessions/{id}?client_id=...` | حذف گفتگو |

---

## ۳. کار با mock

تا وقتی بک‌اند واقعی آماده نشده، `USE_MOCK=true` همین قرارداد را با داده‌ی
ساختگی سرو می‌کند. سه سناریو دارد تا همه‌ی حالت‌های UI قابل ساخت باشد:

| بنویسید | چه اتفاقی می‌افتد |
|---|---|
| `نسخه پایتون رو کجا تعیین کنم؟` | پاسخ عادی + بلوک کد + سه منبع + چیپ‌های قدم بعدی |
| `می‌خوام جنگو دیپلوی کنم` | سؤال تکمیلی + سه چیپ گزینه، بدون منبع |
| `خطا: could not install packages` | دو ابزار پشت سر هم + پاسخ + منبع + چیپ فرآیند |
| `قدم به قدم دیپلوی رو راهنمایی کن` | شروع فرآیند: stepper + قدم اول |
| `قدم بعد` | حرکت واقعی روی همان فرآیند (وضعیت در session می‌ماند) |
| `خروج از فرآیند` | بستن فرآیند و برگشت به حالت عادی |

وقتی بک‌اند آماده شد، فقط `USE_MOCK=false` می‌شود. **هیچ تغییری در فرانت لازم نیست.**
