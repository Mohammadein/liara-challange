# PLAN.md — دستیار LLM مستندات لیارا

> نقشه راه پروژه برای چالش لیارا (۳۰۰ امتیاز)
> مهلت: **جمعه ۲۱ اوت ۲۰۲۶، ساعت ۲۳:۰۰**

---

## ۱. تصویر کلی

```
┌──────────────── BUILD TIME (offline، یک‌بار روی لپ‌تاپ) ────────────────┐
│                                                                        │
│   docs/src/pages/**/*.mdx  (۱۱۴۲ فایل)                                 │
│            │                                                           │
│            ▼  ① Parsing    ← پارسر JSX (نه مارک‌داون!)                  │
│      اسناد ساختاریافته                                                  │
│            │                                                           │
│            ▼  ② Chunking   ← برش روی <Section>، تفکیک <Tabs>            │
│      ~۹۰۰۰ تکه                                                          │
│            │                                                           │
│            ├──▼ ③ Embedding  →  data/vectors.npy   (~۳۷ MB)            │
│            └──▼ ③b BM25      →  data/bm25.pkl      (~۸ MB)             │
│                              →  data/chunks.json   (~۳۰ MB)            │
└────────────────────────────────────────────────────────────────────────┘
                                    │
                          (سه فایل داخل ایمیج commit می‌شوند)
                                    │
┌──────────────── RUN TIME (روی زیرساخت لیارا) ──────────────────────────┐
│                                                                        │
│   Browser ──── POST /api/chat ────►  FastAPI                           │
│                                          │                             │
│                              ┌───────────┴────────────┐                │
│                              │  Rate Limit + Validation│               │
│                              └───────────┬────────────┘                │
│                                          ▼                             │
│                                   Semantic Cache ──hit──► پاسخ فوری    │
│                                          │ miss                        │
│                                          ▼                             │
│                                   Agent Loop (tool calling)            │
│                                     ├─ search_docs ──► Hybrid Search   │
│                                     │                   ├─ vectors.npy │
│                                     │                   └─ bm25.pkl    │
│                                     ├─ generate_liara_json             │
│                                     ├─ diagnose_error                  │
│                                     └─ estimate_cost                   │
│                                          │                             │
│                                          ▼                             │
│                                   LLM  (https://ai.liara.ir)           │
│                                          │                             │
│                                   SSE stream ──► UI                    │
│                                          │                             │
│                                   Structured Logging                   │
└────────────────────────────────────────────────────────────────────────┘
```

**اصل حاکم بر معماری:** هیچ دیتابیس، هیچ سرویس جانبی، هیچ اتصال شبکه‌ای به‌جز خود LLM. یک اپلیکیشن، یک ایمیج، صفر نقطه شکست اضافه.

---

## ۲. استک فنی

| لایه | انتخاب | دلیل |
|---|---|---|
| زبان بک‌اند | Python 3.12 | مهارت تیم |
| وب فریم‌ورک | FastAPI + Uvicorn | استریم SSE بومی، سریع، مورد پشتیبانی لیارا |
| ذخیره وکتور | numpy `.npy` در RAM | ۹۰۰۰ وکتور = یک ضرب ماتریسی زیر میلی‌ثانیه |
| جستجوی کلیدواژه | `rank_bm25` | اسم سرویس‌های خاص فارسی |
| LLM | `ai.liara.ir` (سازگار OpenAI) | امتیاز استقرار + هزینه |
| کتابخانه کلاینت | `openai` (فقط `base_url` عوض می‌شود) | استاندارد، tool calling آماده |
| فرانت | HTML + Tailwind، سرو شده توسط FastAPI | تک‌اپ، بدون CORS، بدون build |
| استقرار | لیارا PaaS (`platform: docker`) | کنترل کامل روی ایمیج |

---

## ۳. ساختار ریپو

```
liara-docs-assistant/
├── ingest/                       ← فقط build time، در ایمیج نمی‌رود
│   ├── parse.py                  ① پارسر JSX
│   ├── chunk.py                  ② تکه‌بندی
│   ├── embed.py                  ③ امبدینگ + کش
│   ├── build_bm25.py             ③b ایندکس کلیدواژه
│   └── run_all.py                اجرای کل خط لوله
│
├── data/                         ← خروجی ingest، commit می‌شود
│   ├── chunks.json
│   ├── vectors.npy
│   └── bm25.pkl
│
├── app/
│   ├── main.py                   FastAPI، روت‌ها، لایف‌سایکل
│   ├── retrieval.py              جستجوی هیبریدی + RRF
│   ├── agent.py                  حلقه tool calling
│   ├── tools.py                  تعریف ابزارها
│   ├── llm.py                    کلاینت ai.liara.ir + retry
│   ├── cache.py                  کش معنایی
│   ├── security.py               rate limit، اعتبارسنجی ورودی
│   ├── logging_conf.py           لاگ JSON ساختاریافته
│   └── prompts.py                system prompt‌ها
│
├── static/
│   ├── index.html                UI چت
│   └── app.js
│
├── eval/
│   ├── golden.jsonl              ۲۵ سؤال + URL منبع مورد انتظار
│   └── run_eval.py               سنجش Recall@5
│
├── liara.json
├── Dockerfile
├── requirements.txt
├── .env.example
└── README.md
```

---

## ۴. فازها

زمان کل: **~۳۰ ساعت**. اعداد ساعت تقریبی و برای اولویت‌گذاری است، نه زمان‌بندی دقیق.

### فاز ۰ — اسکلت و قفل قراردادها (۲ ساعت) — محمدحسین

قبل از هر کد واقعی، دو چیز **نوشته و قفل** می‌شود تا چهار نفر موازی کار کنند:

**قرارداد داده — یک تکه:**
```json
{
  "id": "paas-django-deploy-app#liara-json#cli",
  "text": "متن اصلی که به LLM می‌رود",
  "embed_text": "مسیر + عنوان + بخش + متن",
  "url": "https://docs.liara.ir/paas/django/how-tos/deploy-app/#liara-json",
  "page_title": "استقرار برنامه Django در لیارا",
  "section_title": "فایل liara.json",
  "breadcrumb": "PaaS › Django › راهنماها",
  "service": "paas",
  "method": "Liara CLI",
  "has_code": true
}
```

**قرارداد API:**
```
POST /api/chat
  body: { "message": str, "session_id": str }
  response: text/event-stream
    event: token   data: {"t": "..."}
    event: tool    data: {"name": "search_docs", "status": "running"}
    event: sources data: {"items": [{"title","url","section"}]}
    event: done    data: {"tokens_used": 1234, "cached": false}
```

خروجی این فاز: ریپو ساخته، `requirements.txt` نوشته، و یک **mock API** که داده الکی استریم می‌کند تا ابوالفضل بلافاصله UI را شروع کند بدون اینکه منتظر بک‌اند بماند.

---

### فاز ۶ (زودهنگام!) — دیپلوی روی لیارا (۱ ساعت) — محمدحسین

> ⚠️ این کار **همان ساعت اول** انجام می‌شود، نه شب آخر.

یک FastAPI خالی که فقط `/health` دارد را روی لیارا مستقر کن. `liara.json` و `Dockerfile` را همین‌جا درست کن.

**چرا اول؟** ۴۰ امتیاز استقرار تنها امتیازی است که می‌شود **کاملاً صفر** گرفت. اگر شب آخر تازه بفهمی ایمیج بیلد نمی‌شود یا حجم فایل‌ها مشکل دارد، هیچ کاری نمی‌شود کرد. با دیپلوی زودهنگام، از آن به بعد هر تغییر فقط یک `liara deploy` است.

```json
{
  "app": "liara-docs-assistant",
  "platform": "docker",
  "port": 8000,
  "disks": []
}
```

---

### فاز ۱ — خط لوله Ingest (۵ ساعت) — محمدحسین

**① Parsing** — پارسر JSX، نه مارک‌داون. ترتیب عملیات مهم است:

```
۱. حذف خطوط import
۲. حذف {/* کامنت‌ها */}         ← بحرانی، وگرنه محتوای بیات تزریق می‌شود
۳. استخراج <title> و og:description
۴. استخراج # عنوان H1
۵. <Highlight className="json">{`...`}</Highlight>  →  بلوک کد
۶. <Section id="x" title="y" />                     →  مرز بخش + anchor
۷. <Tabs content={[A,B,C]}>                         →  سه نسخه با برچسب روش
۸. <Step steps={[...]}>                             →  لیست مرحله‌به‌مرحله
۹. <Alert variant="warning">                        →  هشدار (variant حفظ شود)
۱۰. حذف نویز: <div className="h-4"/>, <br/>, <hr/>, <video>
۱۱. حذف تگ‌های باقی‌مانده، حفظ متن داخلی
```

**نگاشت مسیر به URL** (بحرانی‌ترین بخش کل پروژه):
```
src/pages/paas/django/how-tos/deploy-app.mdx
  → https://docs.liara.ir/paas/django/how-tos/deploy-app/
src/pages/ai/about.mdx
  → https://docs.liara.ir/ai/about/
```
حتماً روی نمونه تصادفی ۲۰ فایل تست شود. لینک ۴۰۴ در دمو = از دست دادن امتیاز «ارائه منبع مناسب».

**② Chunking** — `<Section>` مرزهای معنایی را آماده داده، پس تقریباً رایگان است:
- هدف ۳۰۰–۸۰۰ توکن؛ بخش‌های ریز ادغام، بخش‌های درشت روی پاراگراف شکسته
- بلوک کد هرگز وسطش شکسته نمی‌شود
- **غنی‌سازی:** هر تکه با سرآیند `مسیر › صفحه › بخش` امبد می‌شود تا تکه‌های وابسته‌به‌زمینه («سپس ذخیره کنید») قابل بازیابی شوند

**③ Embedding** — batch ۱۰۰تایی، کش بر اساس hash متن، نرمال‌سازی موقع build.

**③b BM25** — با نرمال‌سازی فارسی: `ي→ی`، `ك→ک`، یکسان‌سازی نیم‌فاصله، حذف اعراب.

---

### فاز ۲ — بازیابی و API (۴ ساعت) — محمدحسین

```python
def retrieve(query, k=5, service=None, method=None):
    dense  = vectors @ embed(query)      # شباهت معنایی
    sparse = bm25.get_scores(tokens)     # تطابق کلیدواژه
    return rrf_fuse(dense, sparse)[:k]   # ترکیب رتبه‌ها
```

- **بازنویسی کوئری:** «چطور سایتم رو بالا بیارم» با مدل کوچک به «استقرار برنامه در لیارا» بازنویسی می‌شود
- **فیلتر متادیتا:** اگر context مکالمه سرویس را مشخص کرده، `service=paas` فیلتر شود
- **`/api/chat` با استریم SSE**

---

### فاز ۳ — قابلیت‌های Agentic (۵ ساعت) — کیاراد

**۵۰ امتیاز، و جایی که اکثر تیم‌ها ضعیف‌اند.** چهار ابزار:

| ابزار | کار |
|---|---|
| `search_docs(query, service?, method?)` | جستجوی مستندات |
| `generate_liara_json(platform, options)` | تولید فایل کانفیگ آماده برای پروژه کاربر |
| `diagnose_error(log)` | کاربر لاگ خطای دیپلوی را paste می‌کند، ایجنت تشخیص می‌دهد |
| `estimate_cost(service, plan, usage)` | تخمین هزینه |

**سؤال تکمیلی — امتیاز آسان:** ساختار `<Tabs>` مستندات (Console / CLI / GitHub) یک محور شخصی‌سازی آماده به ما داده. وقتی کاربر می‌پرسد «جنگو دیپلوی کنم»:

> با کدام روش؟ کنسول لیارا، CLI، یا اتصال به GitHub؟

بعد فقط تکه‌های همان روش بازیابی می‌شوند. این هم «درک intent»، هم «سؤال تکمیلی»، هم «شخصی‌سازی» را یکجا می‌گیرد.

**حفظ context:** خلاصه‌ی چرخشی مکالمه + سرویسی که کاربر رویش کار می‌کند در حافظه session.

**پیشنهاد قدم بعدی:** انتهای هر پاسخ، دو تا سه اقدام بعدی مرتبط.

---

### فاز ۴ — رابط کاربری (۸ ساعت، از ساعت ۲ موازی) — ابوالفضل

**۵۵ امتیاز.** روی mock API شروع می‌شود، منتظر بک‌اند نمی‌ماند.

- استریم توکن‌به‌توکن (نه انتظار برای پاسخ کامل)
- **بلوک کد با دکمه کپی و `dir="ltr"`** — مستندات فنی است، این جزئیات دیده می‌شود
- **کارت منابع** زیر هر پاسخ با لینک anchor مستقیم به بخش
- نمایش وضعیت ابزارها («در حال جستجوی مستندات...») — قابلیت Agentic باید **دیده** شود وگرنه داور امتیاز نمی‌دهد
- ریسپانسیو کامل، RTL، تست واقعی روی موبایل
- حالت خطا، حالت خالی، سؤالات پیشنهادی اولیه

---

### فاز ۵ — امنیت، پایداری، Monitoring (۳ ساعت) — کیاراد

**۵۰ امتیاز با کمترین کار.** چک‌لیست:

- [ ] Rate limit به ازای IP و به ازای session
- [ ] همه‌ی secretها فقط از env (`LIARA_AI_API_KEY`)، هیچ کلیدی در ریپو
- [ ] اعتبارسنجی ورودی: سقف طول پیام، sanitize
- [ ] محافظت در برابر prompt injection در system prompt
- [ ] `try/except` روی همه‌ی فراخوانی‌های LLM + retry با backoff
- [ ] پیام خطای معنادار به کاربر وقتی LLM در دسترس نیست
- [ ] لاگ JSON ساختاریافته: `request_id`، latency، توکن مصرفی، cache hit
- [ ] `/health` و `/metrics`
- [ ] سقف `max_tokens` روی هر پاسخ

---

### فاز ۷ — بهینه‌سازی هزینه (۲ ساعت) — کیاراد

**۲۵ امتیاز.** نکته: این کارها باید در README **مستند** شوند وگرنه داور نمی‌بیندشان.

- **کش معنایی:** سؤال جدید اگر شباهت > ۰.۹۵ به سؤال قبلی داشت، پاسخ ذخیره‌شده برگردد
- **کش امبدینگ کوئری**
- **مدل دو سطحی:** مدل کوچک برای بازنویسی کوئری و مسیریابی، مدل بزرگ فقط برای پاسخ نهایی
- **کنترل context:** فقط ۵ تکه برتر، با سقف کاراکتر
- **شمارنده مصرف** در لاگ‌ها تا در دمو بشود نشان داد

---

### مجموعه سؤال طلایی (۱ ساعت، موازی) — حسام

**این کار کوچک، از ۸۰ امتیاز کیفیت پاسخ محافظت می‌کند.**

۲۵ سؤال واقعی با URL منبع مورد انتظار:

```jsonl
{"q": "چطور جنگو رو با CLI دیپلوی کنم؟", "expect_url": ".../paas/django/how-tos/deploy-app/"}
{"q": "نسخه پایتون رو کجا تعیین کنم؟", "expect_url": ".../paas/django/how-tos/deploy-app/#liara-json-version"}
{"q": "baseUrl سرویس هوش مصنوعی چیه؟", "expect_url": ".../ai/quick-start/"}
```

بدون این، وقتی chunking را عوض می‌کنی **نمی‌فهمی بهتر شد یا بدتر** — فقط حدس می‌زنی. با این، یک عدد Recall@5 داری.

ترکیب سؤالات: ۱۰ ساده، ۱۰ پیچیده/چندمرحله‌ای، ۵ مبهم (که ایجنت باید سؤال تکمیلی بپرسد).

---

### فاز ۸ — دمو و README (۳ ساعت) — حسام + همه

- ویدئو **حداقل ۵ دقیقه**، توضیح خود اعضای تیم
- سناریوی کامل، تست روی **موبایل و دسکتاپ** (الزامی)
- نمایش صریح قابلیت‌های Agentic — سؤال تکمیلی، تولید liara.json، تشخیص خطا
- README: معماری، راهنمای اجرا، **و تصمیم‌های بهینه‌سازی هزینه**

---

## ۵. نگاشت فازها به معیارهای داوری

داوران دقیقاً روی این معیارها امتیاز می‌دهند. هر فاز باید بداند کدام عدد را هدف گرفته:

| معیار | امتیاز | فازهای مؤثر |
|---|---|---|
| کیفیت و صحت پاسخ‌ها | ۸۰ | فاز ۱ (parsing/chunking)، فاز ۲ (بازیابی هیبریدی)، سؤال طلایی |
| طراحی UI و تجربه کاربری | ۵۵ | فاز ۴ |
| قابلیت‌های Agentic و Personalization | ۵۰ | فاز ۳ + نمایشش در فاز ۴ |
| امنیت، پایداری و Monitoring | ۵۰ | فاز ۵ |
| استقرار روی زیرساخت لیارا | ۴۰ | فاز ۶ (زودهنگام) |
| بهینه‌سازی هزینه | ۲۵ | فاز ۷ + مستندسازی در README |

---

## ۶. تقسیم کار

| نفر | مسئولیت |
|---|---|
| **محمدحسین** | ingest، retrieval، FastAPI، دیپلوی |
| **کیاراد** | ابزارهای ایجنت، امنیت، کش، لاگ |
| **ابوالفضل** | کل UI (تنها فرانت‌کار تیم — بحرانی‌ترین منبع) |
| **حسام** | سؤالات طلایی، تست سناریوها، ضبط و تدوین ویدئو |

---

## ۷. ریسک‌ها

| ریسک | شدت | کاهش |
|---|---|---|
| نگاشت مسیر→URL غلط، لینک ۴۰۴ در دمو | بالا | تست روی ۲۰ فایل تصادفی در فاز ۱ |
| کامنت‌های `{/* */}` پارس شوند و محتوای بیات تزریق شود | بالا | قدم ۲ پارسر، حتماً قبل از بقیه |
| ابوالفضل تنها فرانت‌کار = گلوگاه | بالا | mock API در فاز ۰ تا از ساعت ۲ شروع کند |
| شکست دیپلوی در شب آخر | بالا | فاز ۶ زودهنگام، ساعت اول |
| زمان/هزینه امبدینگ ۹۰۰۰ تکه | متوسط | کش بر اساس hash؛ اگر کند بود، اول `paas` و `ai` را ingest کن |
| افتادن در دام over-engineering | متوسط | معیار: هر فیچر باید به یک عدد از جدول بخش ۵ وصل باشد |

---

## ۸. حداقل محصول قابل قبول

اگر زمان کم آمد، **این ترتیب** حفظ شود:

1. ✅ دیپلوی‌شده روی لیارا و بالا (۴۰)
2. ✅ پاسخ درست با منبع درست (۸۰)
3. ✅ UI تمیز و ریسپانسیو (۵۵)
4. ✅ rate limit + مدیریت خطا + لاگ (۵۰)
5. ⬜ ابزارهای Agentic (۵۰)
6. ⬜ کش و بهینه‌سازی هزینه (۲۵)

یک محصول ساده که **کار می‌کند و روی لیارا بالاست** از یک محصول بلندپروازانه که روی لپ‌تاپ مانده، امتیاز خیلی بیشتری می‌گیرد.
